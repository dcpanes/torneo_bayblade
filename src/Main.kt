// === Acá van las excepciones ===
// recuerda que deberías manejar excepciones 😉


// === Acá van las funciones ===
fun puntajeBase(potencia: Int, precision: Double, poderBase: Int): Int {
    // Crea la lógica de está función
    return 0
}

fun bonusFrase(frase: String): Int {
    // Crea la lógica de está función
    return 0
}

fun ganadorRonda(puntajeVillano: Int, puntajeColeccionista: Int): String {
    // Crea la lógica de está función
    return ""
}


// === MAIN ===
fun main() {

    // Instancia a Villano y Coleccionista

    // Define los datos de la ronda única

    // Calcula los puntajes finales

    // Imprime el resultado

    // Recuerda que estás manejando excepciones así que debes usar tr .... * se muere el profe *

}

// A pastar!